apiVersion: v1
kind: Service
metadata:
  name: mlflow-postgres-postgresql
  namespace: application
spec:
  type: ClusterIP
  selector:
    app: mlflow-postgres
  ports:
    - port: 5432
      targetPort: 5432
